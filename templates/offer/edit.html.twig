{% extends 'base.html.twig' %}

{% block title %}Edit Offer
{% endblock %}

{% block body %}
	<main class="mainSection">

		<div class="mainForm">
			<h1>Editer l'offre</h1>
			{{ include('offer/_form.html.twig', {'button_label': 'Editer mon offre'}) }}
			<a href="{{ path('app_offer_index') }}">back to list</a>
			{{ include('offer/_delete_form.html.twig') }}
		</div>

	</main>
{% endblock %}
