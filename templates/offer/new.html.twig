{% extends 'base.html.twig' %}

{% block title %}Nouvelle offre
{% endblock %}

{% block body %}
	<main class="mainSection mb-5">
		<div class="mainForm">
			<h1 class="text-center">Publier une offre</h1>
			{% if no_stacks is defined %}
				<p>{{ no_stacks }}</p>
			{% endif %}

			{{ include('offer/_form.html.twig') }}
		</div>
	</main>
{% endblock %}
